{
  "name": "@ly-public/router-dom-wrapper",
  "version": "0.1.8",
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/lanyanxiang/router-dom-wrapper"
  },
  "description": "Wrapper around React router DOM to achieve public/private routes and configuration-based routing.",
  "main": "dist/index.js",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "chmod +x scripts/build.sh && scripts/build.sh",
    "clean": "del ./dist/*",
    "lint": "eslint .",
    "prepublishOnly": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jimmy Lan",
  "license": "MIT",
  "devDependencies": {
    "@types/react-router-dom": "^5.3.1",
    "@typescript-eslint/eslint-plugin": "^5.1.0",
    "del-cli": "^3.0.1",
    "eslint": "^8.1.0",
    "prettier": "^2.4.1",
    "typescript": "^4.1.2"
  },
  "peerDependencies": {
    "react": ">= 16",
    "react-router-dom": ">= 5 < 6"
  }
}
